<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zsc.module.mapper.BizCategoryMapper">

    <resultMap id="BizCategoryVoResultMap" type="com.zsc.module.domain.vo.BizCategoryVo">
        <id property="categoryId" column="category_id"/>
        <result property="categoryName" column="category_name"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <sql id="selectCategoryVo">
        SELECT category_id, category_name, sort_order, status, create_time, update_time
        FROM biz_category
        WHERE 1=1
    </sql>

    <sql id="whereClause">
        <if test="query.categoryName != null and query.categoryName != ''">
            AND category_name LIKE CONCAT('%', #{query.categoryName}, '%')
        </if>
        <if test="query.status != null and query.status != ''">
            AND status = #{query.status}
        </if>
    </sql>

    <!-- 分页查询类别列表 -->
    <select id="selectCategoryVoPage" resultMap="BizCategoryVoResultMap">
        <include refid="selectCategoryVo"/>
        <include refid="whereClause"/>
        ORDER BY sort_order ASC, create_time DESC
    </select>

    <!-- 查询类别详情 -->
    <select id="selectCategoryVoById" resultMap="BizCategoryVoResultMap">
        <include refid="selectCategoryVo"/>
        AND category_id = #{categoryId}
    </select>

    <!-- 统计关联酒店数量（暂无直接关联，返回0） -->
    <select id="countHotelsByCategoryId" resultType="java.lang.Long">
        SELECT 0
    </select>

</mapper>
