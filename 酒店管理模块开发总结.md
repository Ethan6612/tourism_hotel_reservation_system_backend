# 酒店管理模块后端开发总结

> 开发日期：2026-06-12
> 开发人员：lyx
> 模块：酒店管理模块（业务分类、酒店、房型）

---

## 一、开发概览

### 1.1 完成内容

| 阶段 | 内容 | 状态 |
|------|------|------|
| 数据库设计 | new_zsc.sql（新增字段） | ✅ |
| 实体类 | Hotel、Room、BizCategory | ✅ |
| 状态枚举 | 酒店/房型/分类/删除标志枚举 | ✅ |
| DTO/VO | 入参/出参/分页对象 | ✅ |
| Mapper | 接口 + XML | ✅ |
| Service | 业务逻辑层 | ✅ |
| Controller | 接口层 | ✅ |
| API文档 | 接口文档 | ✅ |

### 1.2 接口统计

| 模块 | 接口数量 |
|------|----------|
| 业务类别管理 | 7 |
| 酒店管理 | 9 |
| 房型管理 | 11 |
| **合计** | **27** |

---

## 二、数据库改动

### 2.1 表结构变更

**hotel 表新增字段：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| phone | varchar(20) | 酒店联系电话 |
| intro | text | 酒店简介 |
| longitude | decimal(10,6) | 经度 |
| latitude | decimal(10,6) | 纬度 |
| del_flag | char(1) | 删除标志(0正常 2删除) |

**room 表新增字段：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| guest_num | int | 可入住人数 |
| has_breakfast | char(1) | 是否含早餐(0无 1有) |
| del_flag | char(1) | 删除标志(0正常 2删除) |

**biz_category 表新增字段：**

| 字段名 | 类型 | 说明 |
|--------|------|------|
| del_flag | char(1) | 删除标志(0正常 2删除) |

### 2.2 状态编码规范

| 表 | 字段 | 值 | 说明 |
|----|------|-----|------|
| biz_category | status | 0 | 正常 |
| | | 1 | 停用 |
| hotel | status | 0 | 营业中 |
| | | 1 | 草稿 |
| | | 2 | 待审核 |
| | | 3 | 已下架 |
| | | 4 | 已冻结 |
| room | status | 0 | 上架 |
| | | 1 | 下架 |
| 通用 | del_flag | 0 | 正常 |
| | | 2 | 已删除 |

---

## 三、新增/修改文件清单

### 3.1 数据库

```
sql/
└── new_zsc.sql                          # 新版数据库脚本
```

### 3.2 实体类

```
zsc-module/src/main/java/com/zsc/module/domain/entity/
├── BizCategory.java                     # 业务分类实体（更新）
├── Hotel.java                           # 酒店实体（新增）
└── Room.java                            # 房型实体（新增）
```

### 3.3 状态枚举类

```
zsc-module/src/main/java/com/zsc/module/domain/enums/
├── CategoryStatusEnum.java              # 分类状态枚举
├── HotelStatusEnum.java                 # 酒店状态枚举
├── RoomStatusEnum.java                  # 房型状态枚举
└── DelFlagEnum.java                     # 删除标志枚举
```

### 3.4 DTO（请求入参）

```
zsc-module/src/main/java/com/zsc/module/domain/dto/
├── HotelDTO.java                        # 酒店新增/编辑入参
├── RoomDTO.java                         # 房型新增/编辑入参
├── HotelSearchDTO.java                  # 酒店多维度搜索入参
└── query/
    ├── HotelQueryDto.java               # 酒店后台查询参数
    └── RoomQueryDto.java                # 房型后台查询参数
```

### 3.5 VO（响应出参）

```
zsc-module/src/main/java/com/zsc/module/domain/vo/
├── BizCategoryVo.java                   # 分类展示对象（更新）
├── HotelListVO.java                     # 酒店列表精简展示
├── HotelDetailVO.java                   # 酒店详情完整对象
├── HotelVo.java                         # 酒店VO
└── RoomVo.java                          # 房型展示对象（更新）
```

### 3.6 Mapper

```
zsc-module/src/main/java/com/zsc/module/mapper/
├── BizCategoryMapper.java               # 分类Mapper（更新）
├── HotelMapper.java                     # 酒店Mapper（新增）
└── RoomMapper.java                      # 房型Mapper（新增）

zsc-module/src/main/resources/mapper/module/
├── BizCategoryMapper.xml                # 分类SQL（新增）
├── HotelMapper.xml                      # 酒店SQL（新增）
└── RoomMapper.xml                       # 房型SQL（新增）
```

### 3.7 Service

```
zsc-module/src/main/java/com/zsc/module/service/
├── BizCategoryService.java              # 分类服务（更新）
├── HotelService.java                    # 酒店服务（新增）
├── RoomService.java                     # 房型服务（新增）
├── OrderService.java                    # 订单服务（更新）
└── impl/
    ├── BizCategoryServiceImpl.java      # 分类服务实现（更新）
    ├── HotelServiceImpl.java            # 酒店服务实现（新增）
    ├── RoomServiceImpl.java             # 房型服务实现（新增）
    └── OrderServiceImpl.java            # 订单服务实现（更新）
```

### 3.8 Controller

```
zsc-module/src/main/java/com/zsc/module/controller/
├── BizCategoryController.java           # 分类控制器（更新）
├── HotelController.java                 # 酒店控制器（新增）
└── RoomController.java                  # 房型控制器（新增）
```

### 3.9 文档

```
docs/
└── 酒店管理模块API接口文档.md              # API接口文档
```

---

## 四、核心功能实现

### 4.1 业务分类模块

| 功能 | 说明 |
|------|------|
| 新增/编辑 | 名称唯一性校验 |
| 删除 | 校验是否关联酒店，关联则禁止删除 |
| 状态管理 | 启用/停用，停用前校验关联酒店 |
| 分页查询 | 支持名称模糊查询、状态筛选 |
| 下拉查询 | 只返回启用状态的分类 |

### 4.2 酒店模块

| 功能 | 说明 |
|------|------|
| 新增/编辑 | 字段校验（名称非空、星级1-5、经纬度范围） |
| 删除 | 校验是否有关联房型 |
| 状态管理 | 草稿→待审核→营业中→下架/冻结 |
| 上架申请 | 草稿状态提交审核，校验必填字段 |
| 后台查询 | 全状态分页查询 |
| 前台搜索 | 多维度搜索，只展示营业中酒店 |
| 详情查询 | 关联房型、最低价格、设施列表 |

### 4.3 房型模块

| 功能 | 说明 |
|------|------|
| 新增/编辑 | 参数校验、酒店存在性校验 |
| 删除 | 校验是否有未完成订单 |
| 上下架 | 状态切换 |
| 价格管理 | 修改价格，校验价格>0 |
| 库存管理 | 单条/批量修改，校验库存≥0 |
| 库存预警 | 筛选低库存房型 |
| 库存操作 | 锁定/扣减/释放（对接订单模块） |

---

## 五、业务规则

### 5.1 酒店状态流转

```
草稿(1) → 待审核(2) → 营业中(0) → 已下架(3) → 草稿(1)
                      ↓            ↓
                   已冻结(4) ←─────┘
                      ↓
                   已下架(3)
```

### 5.2 数据权限

| 场景 | 数据范围 |
|------|----------|
| 后台管理 | 全状态酒店 |
| 前台用户 | 只展示营业中酒店 |
| 房型列表 | 只展示上架状态 |

### 5.3 删除校验

| 模块 | 校验规则 |
|------|----------|
| 分类删除 | 存在关联酒店则禁止 |
| 酒店删除 | 存在关联房型则禁止 |
| 房型删除 | 存在未完成订单则禁止 |

---

## 六、接口清单

### 6.1 业务类别管理

| 方法 | 接口 | 说明 |
|------|------|------|
| GET | /api/category/{categoryId} | 获取分类详情 |
| POST | /api/category | 新增分类 |
| PUT | /api/category | 编辑分类 |
| DELETE | /api/category/{categoryId} | 删除分类 |
| PUT | /api/category/{categoryId}/status/{status} | 修改状态 |
| POST | /api/category/query | 分页查询 |
| GET | /api/category/select | 下拉查询 |

### 6.2 酒店管理

| 方法 | 接口 | 说明 |
|------|------|------|
| GET | /api/hotel/{id} | 获取酒店详情 |
| POST | /api/hotel | 新增酒店 |
| PUT | /api/hotel | 编辑酒店 |
| DELETE | /api/hotel/{id} | 删除酒店 |
| PUT | /api/hotel/{id}/status/{status} | 修改状态 |
| PUT | /api/hotel/{id}/submitAudit | 提交上架申请 |
| POST | /api/hotel/query | 后台管理列表 |
| POST | /api/hotel/search | 多维度搜索 |
| GET | /api/hotel/detail/{id} | 酒店详情查询 |

### 6.3 房型管理

| 方法 | 接口 | 说明 |
|------|------|------|
| GET | /api/room/{id} | 获取房型详情 |
| POST | /api/room | 新增房型 |
| PUT | /api/room | 编辑房型 |
| DELETE | /api/room/{id} | 删除房型 |
| PUT | /api/room/{id}/status/{status} | 上下架 |
| PUT | /api/room/{id}/price | 修改价格 |
| PUT | /api/room/{id}/stock | 修改库存 |
| PUT | /api/room/batchStock | 批量修改库存 |
| GET | /api/room/lowStock | 库存预警查询 |
| POST | /api/room/query | 分页查询 |
| GET | /api/room/hotel/{hotelId} | 按酒店查房型 |

---

## 七、技术要点

### 7.1 使用的注解

| 注解 | 说明 |
|------|------|
| @TableName | MyBatis-Plus 表名映射 |
| @TableId | 主键映射 |
| @TableLogic | 逻辑删除 |
| @JsonFormat | JSON日期格式化 |
| @Valid | 参数校验 |
| @PreAuthorize | 权限控制 |
| @Log | 操作日志 |
| @Lazy | 解决循环依赖 |

### 7.2 解决的问题

| 问题 | 解决方案 |
|------|----------|
| 循环依赖 | 使用 @Lazy 注解 |
| 状态管理 | 枚举类统一管理 |
| 逻辑删除 | @TableLogic + del_flag字段 |
| 分页查询 | MyBatis-Plus 分页插件 |

---

## 八、后续待完成

| 功能 | 说明 |
|------|------|
| 价格日历 | 季节价/活动价配置 |
| 库存日历 | 单日库存设置 |
| 订单对接 | 库存锁定/释放完整联调 |
| 评价模块 | 酒店评价展示 |
| 图片上传 | 酒店/房型图片管理 |

---

## 九、文件统计

| 类型 | 数量 |
|------|------|
| 数据库脚本 | 1 |
| 实体类 | 3 |
| 枚举类 | 4 |
| DTO | 5 |
| VO | 5 |
| Mapper接口 | 3 |
| Mapper XML | 3 |
| Service接口 | 4 |
| Service实现 | 4 |
| Controller | 3 |
| 文档 | 2 |
| **合计** | **37** |

---

## 十、启动说明

1. 执行 `sql/new_zsc.sql` 初始化数据库
2. 启动 `ZscApplication` 主类
3. 访问 Swagger 文档：`http://localhost:8080/swagger-ui.html`
4. 访问 API 文档：查看 `docs/酒店管理模块API接口文档.md`

---

*文档生成时间：2026-06-12*
